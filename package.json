{
  "name": "habit-tracker",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "dev": "concurrently \"tsup src/server.ts --watch --format esm --outDir dist\" \"node --watch dist/server.mjs\"",
  "build": "tsup src/server.ts --format esm --outDir dist"
}

,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@hono/node-server": "^1.18.1",
    "better-sqlite3": "^12.2.0",
    "drizzle-orm": "^0.44.4",
    "hono": "^4.8.12"
  },
  "devDependencies": {
    "@types/node": "^24.2.0",
    "concurrently": "^9.2.0",
    "esbuild": "^0.25.8",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
